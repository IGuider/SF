{
  "name": "simplefinance",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:pretty": "astro build && prettier --write dist --ignore-unknown",
    "format": "prettier --write . --ignore-unknown",
    "test": "node --test tests/*.test.*",
    "check": "npm run test && npm run build",
    "preview": "astro preview",
    "astro": "astro",
    "publish:pages": "node ./scripts/publish-pages.mjs"
  },
  "dependencies": {
    "astro": "^6.3.6",
    "embla-carousel": "^8.6.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "prettier": "^3.8.2",
    "sass": "^1.98.0"
  }
}
